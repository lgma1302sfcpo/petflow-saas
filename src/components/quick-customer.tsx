"use client";import { useQueryClient } from "@tanstack/react-query";import { FormEvent,useState } from "react";
export function QuickCustomer(){const client=useQueryClient(),[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,data=Object.fromEntries(new FormData(form));const customerResponse=await fetch("/api/customers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)}),customer=await customerResponse.json();if(!customerResponse.ok){setMessage(customer.error);return}if(data.petName){await fetch("/api/pets",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({customerId:customer.customer.id,name:data.petName,type:data.petType,breed:data.petBreed||undefined})})}setMessage("Cliente cadastrado e disponível no PDV.");form.reset();client.invalidateQueries()}return <form className="card section-card module-form" onSubmit={submit} style={{marginTop:18}}><div className="section-title"><div><span className="eyebrow">Sem sair do PDV</span><h2>Cadastro rápido de cliente e pet</h2></div></div><div className="form-row"><div className="field"><label>Cliente</label><input className="input" name="fullName" required/></div><div className="field"><label>WhatsApp</label><input className="input" name="phone" required/></div><div className="field"><label>Pet</label><input className="input" name="petName"/></div><div className="field"><label>Tipo</label><select className="input" name="petType"><option value="DOG">Cão</option><option value="CAT">Gato</option><option value="OTHER">Outro</option></select></div><div className="field"><label>Raça</label><input className="input" name="petBreed"/></div></div>{message?<span className="pill">{message}</span>:null}<button className="button button-ghost">Cadastrar rapidamente</button></form>}
