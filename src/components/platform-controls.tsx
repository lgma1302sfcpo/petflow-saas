"use client";
import { useState } from "react";
import { MODULES } from "@/lib/modules";

type TenantOption={id:string;name:string;status:string};
export function PlatformControls({tenants}:{tenants:TenantOption[]}){const [message,setMessage]=useState("");async function updateStatus(formData:FormData){const tenantId=String(formData.get("tenantId"));const response=await fetch(`/api/platform/tenants/${tenantId}/status`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:formData.get("status"),reason:formData.get("reason")})});const result=await response.json();setMessage(response.ok?"Status atualizado e auditado.":result.error);if(response.ok)location.reload()}async function updateModule(formData:FormData){const tenantId=String(formData.get("tenantId"));const response=await fetch(`/api/platform/tenants/${tenantId}/modules`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({moduleKey:formData.get("moduleKey"),enabled:formData.get("enabled")==="true",reason:formData.get("reason")})});const result=await response.json();setMessage(response.ok?"Módulo atualizado para a empresa.":result.error);if(response.ok)location.reload()}async function updateFeatureFlag(formData:FormData){const tenantId=String(formData.get("tenantId"));const raw=String(formData.get("value"));let value:string|number|boolean|null;if(formData.get("remove")==="true")value=null;else if(raw==="true"||raw==="false")value=raw==="true";else if(raw.trim()!==""&&!Number.isNaN(Number(raw)))value=Number(raw);else value=raw;const response=await fetch(`/api/platform/tenants/${tenantId}/feature-flags`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({key:formData.get("key"),value,reason:formData.get("reason")})});const result=await response.json();setMessage(response.ok?"Funcionalidade personalizada salva para a empresa.":result.error)}return <section className="section-card card"><div className="section-title"><div><span className="eyebrow">Controle central</span><h2>Status e módulos</h2></div></div><form action={updateStatus} className="login-form"><div className="field"><label>Empresa</label><select className="input" name="tenantId">{tenants.map(t=><option key={t.id} value={t.id}>{t.name} · {t.status}</option>)}</select></div><div className="field"><label>Novo status</label><select className="input" name="status"><option value="ACTIVE">Ativa</option><option value="TRIAL">Em teste</option><option value="SUSPENDED">Suspensa</option><option value="CANCELLED">Cancelada</option></select></div><div className="field"><label>Motivo</label><input className="input" name="reason" minLength={5} required/></div><button className="button button-ghost">Atualizar status</button></form><hr style={{border:0,borderTop:"1px solid var(--line)",margin:"22px 0"}}/><form action={updateModule} className="login-form"><div className="field"><label>Empresa</label><select className="input" name="tenantId">{tenants.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></div><div className="field"><label>Módulo</label><select className="input" name="moduleKey">{MODULES.map(module=><option key={module}>{module}</option>)}</select></div><div className="field"><label>Disponibilidade</label><select className="input" name="enabled"><option value="true">Ativar</option><option value="false">Desativar</option></select></div><div className="field"><label>Motivo</label><input className="input" name="reason" minLength={5} required/></div><button className="button button-ghost">Salvar módulo</button></form><hr style={{border:0,borderTop:"1px solid var(--line)",margin:"22px 0"}}/><form action={updateFeatureFlag} className="login-form"><div className="section-title"><div><span className="eyebrow">Particularidades contratadas</span><h2>Funcionalidade personalizada</h2></div></div><div className="field"><label>Empresa</label><select className="input" name="tenantId">{tenants.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></div><div className="field"><label>Chave</label><input className="input" name="key" placeholder="ex: sales.skip-cash-session" minLength={2} required/></div><div className="field"><label>Valor</label><input className="input" name="value" placeholder="true, um número ou texto"/></div><label><input type="checkbox" name="remove" value="true"/> Remover esta chave (ignora o valor acima)</label><div className="field"><label>Motivo</label><input className="input" name="reason" minLength={5} required/></div><button className="button button-ghost">Salvar funcionalidade</button><small className="muted">Fica salvo só para essa empresa, sem afetar as outras. O código lê com <code>hasCustomFeature</code>/<code>getCustomFeature</code> de <code>@/lib/custom-features</code>.</small></form>{message?<p className="pill" style={{marginTop:14}}>{message}</p>:null}</section>}

export function CreatePlanForm(){const [message,setMessage]=useState("");async function submit(formData:FormData){const modules=formData.getAll("enabledModules");const response=await fetch("/api/platform/plans",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:formData.get("name"),slug:formData.get("slug"),maxBranches:formData.get("maxBranches"),maxUsers:formData.get("maxUsers"),enabledModules:modules,enabledFeatures:[],active:true})});const result=await response.json();setMessage(response.ok?"Plano criado.":result.error);if(response.ok)location.reload()}return <form action={submit} className="section-card card"><div className="section-title"><div><span className="eyebrow">Catálogo comercial</span><h2>Novo plano</h2></div></div><div className="login-form"><div className="field"><label>Nome</label><input className="input" name="name" required/></div><div className="field"><label>Identificador</label><input className="input" name="slug" required/></div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}><div className="field"><label>Máx. lojas</label><input className="input" name="maxBranches" type="number" min="1" required/></div><div className="field"><label>Máx. usuários</label><input className="input" name="maxUsers" type="number" min="1" required/></div></div><div className="field"><label>Módulos</label><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:8}}>{MODULES.map(module=><label key={module} style={{fontWeight:500}}><input type="checkbox" name="enabledModules" value={module}/> {module}</label>)}</div></div><button className="button button-ghost">Criar plano</button>{message?<span className="pill">{message}</span>:null}</div></form>}
