import { getServerSession } from "next-auth";import { authOptions } from "@/lib/auth";import { prisma } from "@/lib/prisma";
export async function GET(request:Request){const session=await getServerSession(authOptions);if(session?.user.kind!=="PLATFORM")return Response.json({error:"Não autorizado."},{status:401});const p=new URL(request.url).searchParams,tenantId=p.get("tenantId")||undefined,action=p.get("action")||undefined;const logs=await prisma.auditLog.findMany({where:{tenantId,action:action?{contains:action,mode:"insensitive"}:undefined},orderBy:{createdAt:"desc"},take:Math.min(500,Number(p.get("limit")??100))});return Response.json({logs})}
